# Optional: only set this when you want Android builds to reach a self-hosted sync-server
# or a deliberate legacy backend compatibility endpoint.
VITE_ANDROID_API_BASE_URL=http://192.168.1.100:8000/api
